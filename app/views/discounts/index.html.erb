<%= render partial: "shared/nav" %>
</br>
<h5>Upcoming Holidays: </h5>
<% @holidays.each do |holiday| %>
  <li><%= holiday[:name] %>, <%= holiday[:date] %></li>
<% end %>

</br>
<% @merchant.bulk_discounts.each do |discount| %>
  <h3>Get <%= discount.percentage_discount.round(0) %>% off when you buy <%= discount.quantity_threshold%> or more <%= link_to "items", merchant_discount_path(@merchant, discount)%>! </h3><%= button_to "Delete", merchant_discount_path(@merchant, discount), method: :delete%></br>
<%end%>

<%= link_to "Create a New Discount", new_merchant_discount_path(@merchant) %>
